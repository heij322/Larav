from docx import Document
from docx.shared import Pt, Inches
from docx.enum.text import WD_ALIGN_PARAGRAPH

def add_heading(doc, text, level):
    # Добавляем заголовок
    heading = doc.add_heading(text, level=level)
    for run in heading.runs:
        run.font.name = 'Times New Roman'
        run.font.size = Pt(14 if level > 1 else 16) # Размер шрифта для заголовков
        run.bold = True

def add_paragraph(doc, text, bold=False, size=14, align='justify'):
    p = doc.add_paragraph()
    run = p.add_run(text)
    run.font.name = 'Times New Roman'
    run.font.size = Pt(size)
    run.bold = bold
    if align == 'center':
        p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    elif align == 'justify':
        p.alignment = WD_ALIGN_PARAGRAPH.JUSTIFY
    # Отступ первой строки (абзац)
    p.paragraph_format.first_line_indent = Inches(0.5)
    p.paragraph_format.line_spacing = 1.5

def create_table(doc, headers, rows):
    table = doc.add_table(rows=1, cols=len(headers))
    table.style = 'Table Grid'
    
    # Заголовки
    hdr_cells = table.rows[0].cells
    for i, header in enumerate(headers):
        hdr_cells[i].text = header
        # Делаем заголовки жирными
        for paragraph in hdr_cells[i].paragraphs:
            for run in paragraph.runs:
                run.bold = True

    # Данные
    for row_data in rows:
        row_cells = table.add_row().cells
        for i, cell_data in enumerate(row_data):
            row_cells[i].text = str(cell_data)

doc = Document()

# ------------------- НАЧАЛО ДОКУМЕНТА -------------------

# Титульный лист (простой вариант, обычно заполняется вручную по методичке)
add_paragraph(doc, "КУРСОВАЯ РАБОТА", bold=True, size=16, align='center')
add_paragraph(doc, "по дисциплине: ПМ.09 Проектирование, разработка и оптимизация веб-приложений", size=12, align='center')
add_paragraph(doc, "на тему: «Разработка интернет-магазина фермерских продуктов»", size=12, align='center')
doc.add_page_break()

# Содержание
add_heading(doc, "Содержание", 1)
add_paragraph(doc, "Введение", bold=True)
add_paragraph(doc, "1. Теоретические основы разработки веб-приложений", bold=True)
add_paragraph(doc, "1.1 Архитектурный паттерн MVC в веб-разработке")
add_paragraph(doc, "1.2 Особенности использования PHP-фреймворка Laravel")
add_paragraph(doc, "1.3 Система шаблонов Blade и CSS-стилизация")
add_paragraph(doc, "1.4 Проектирование реляционных баз данных")
add_paragraph(doc, "2. Практическая часть. Разработка интернет-магазина фермерских продуктов", bold=True)
add_paragraph(doc, "2.1 Анализ предметной области и обоснование выбора технологий")
add_paragraph(doc, "2.2 Настройка проектной среды и проектирование базы данных")
add_paragraph(doc, "2.3 Реализация системы аутентификации и модуля магазина")
add_paragraph(doc, "2.4 Разработка административной панели и тестирование приложения")
add_paragraph(doc, "Заключение", bold=True)
add_paragraph(doc, "Список использованных источников", bold=True)
add_paragraph(doc, "Приложения", bold=True)
doc.add_page_break()

# Введение
add_heading(doc, "Введение", 1)
text_intro = (
    "В современном мире веб-приложения стали неотъемлемой частью повседневной жизни миллионов людей. "
    "Развитие интернета, облачных вычислений и высокоскоростных каналов связи привело к тому, что значительная часть торговли переходит в браузерную среду. "
    "Такой подход избавляет покупателей от необходимости посещать физические магазины, обеспечивает доступ к ассортименту с любого устройства и упрощает процесс выбора товаров. "
    "Особое место в сфере электронной коммерции занимают нишевые проекты, такие как интернет-магазины фермерских продуктов, где требуется не только удобный интерфейс каталога, но и инструменты управления заказами для администратора."
    "\n\nРынок фермерской продукции характеризуется специфическими требованиями: необходимость информирования покупателя о натуральности товаров, управление ограниченными партиями товара и обеспечение оперативной доставки скоропортящихся продуктов. "
    "Существующие универсальные платформы часто избыточны для малого бизнеса, а специализированные решения имеют высокую стоимость. "
    "Процесс создания собственного интернет-магазина на базе современного фреймворка позволяет разработать решение, точно соответствующее потребностям конкретного фермерского хозяйства."
    "\n\nАктуальность темы курсовой работы обусловлена растущим спросом на качественные продукты питания и развитием малого бизнеса в сфере агротуризма и фермерства. "
    "Объектом исследования является процесс разработки веб-приложений средствами языка программирования PHP. "
    "Предметом исследования выступает архитектура и интерфейс интернет-магазина фермерских продуктов."
    "\n\nЦель курсовой работы заключается в проектировании и разработке функционального веб-приложения на базе PHP-фреймворка Laravel."
    "\n\nДля достижения поставленной цели были сформулированы следующие задачи:"
)
add_paragraph(doc, text_intro)
tasks = [
    "изучить теоретические основы архитектурного паттерна MVC и принципы работы фреймворка Laravel;",
    "провести анализ предметной области и обосновать выбор технологического стека;",
    "спроектировать реляционную базу данных MySQL для хранения товаров, категорий, пользователей и заказов;",
    "реализовать систему аутентификации и авторизации пользователей;",
    "разработать пользовательский интерфейс с использованием шаблонизатора Blade;",
    "программно реализовать логику работы корзины и оформления заказа;",
    "разработать административную панель управления магазином с системой ролей пользователей."
]
for task in tasks:
    p = doc.add_paragraph(task, style='List Bullet')
    for run in p.runs:
        run.font.name = 'Times New Roman'
        run.font.size = Pt(14)

doc.add_page_break()

# Глава 1
add_heading(doc, "1. Теоретические основы разработки веб-приложений", 1)

add_heading(doc, "1.1 Архитектурный паттерн MVC в веб-разработке", 2)
text_11 = (
    "Современная веб-разработка требует структурированного подхода к написанию кода, обеспечивающего разделяемость ответственности, тестируемость и масштабируемость приложения. "
    "Одним из наиболее распространенных архитектурных решений является паттерн Model-View-Controller (MVC), который разделяет логику приложения на три взаимосвязанных компонента: Модель, Представление и Контроллер."
    "\n\nМодель (Model) отвечает за работу с данными и бизнес-логику приложения. В контексте интернет-магазина модель представляет собой программную абстракцию сущности предметной области, например, «Товар» (Product), «Категория» (Category) или «Заказ» (Order). "
    "Модель инкапсулирует все операции, связанные с данными: создание новых записей, чтение, обновление и удаление (CRUD-операции)."
    "\n\nПредставление (View) отвечает за отображение данных пользователю. Оно содержит HTML-разметку, стили CSS и минимальную логику для вывода переменных, переданных из контроллера. "
    "В фреймворке Laravel для этой цели используется шаблонизатор Blade, который позволяет создавать иерархию шаблонов с механизмом наследования."
    "\n\nКонтроллер (Controller) выступает посредником между Моделью и Представлением. Он принимает HTTP-запросы от пользователя, обрабатывает входные данные, обращается к моделям для получения или сохранения данных и передает результат в представление для рендеринга страницы."
)
add_paragraph(doc, text_11)

add_heading(doc, "1.2 Особенности использования PHP-фреймворка Laravel", 2)
text_12 = (
    "Для реализации курсовой работы был выбран фреймворк Laravel, созданный Тейлором Отуэллом в 2011 году. "
    "Laravel представляет собой одну из наиболее популярных платформ для разработки веб-приложений на языке PHP."
    "\n\nКлючевым компонентом Laravel является ORM Eloquent, позволяющая работать с базами данных через объектно-ориентированные модели вместо сырых SQL-запросов. "
    "Система миграций Laravel представляет собой механизм версионирования структуры базы данных с использованием PHP-кода. "
    "Шаблонизатор Blade является основным средством создания интерфейса в Laravel."
    "\n\nLaravel обеспечивает многоуровневую безопасность. Защита от CSRF реализуется через автоматическую генерацию уникального токена для каждой формы. "
    "Консольная утилита Artisan предоставляет набор команд для ускорения разработки."
)
add_paragraph(doc, text_12)

add_heading(doc, "1.3 Система шаблонов Blade и CSS-стилизация", 2)
text_13 = (
    "Система шаблонов Blade является встроенным шаблонизатором фреймворка Laravel. "
    "В рамках проекта система шаблонов Blade используется для условного отображения элементов интерфейса. "
    "Директива «@auth» отображает ссылки на личный кабинет и администрирование только авторизованным пользователям. "
    "Для стилизации интерфейса используется каскадная таблица стилей (CSS). Дизайн выполнен в «фермерском» стиле с использованием приглушенных зеленых и бежевых тонов."
)
add_paragraph(doc, text_13)

add_heading(doc, "1.4 Проектирование реляционных баз данных", 2)
text_14 = (
    "Основой любого веб-приложения является база данных. При разработке интернет-магазина выбрана реляционная модель. "
    "Нормализация проводилась для исключения избыточности данных. Ссылочная целостность обеспечивается внешними ключами. "
    "MySQL выбрана как надежная СУБД, совместимая с PHP через драйвер PDO."
)
add_paragraph(doc, text_14)

doc.add_page_break()

# Глава 2
add_heading(doc, "2. Практическая часть. Разработка интернет-магазина фермерских продуктов", 1)

add_heading(doc, "2.1 Анализ предметной области и обоснование выбора технологий", 2)
text_21 = (
    "Предметной областью является продажа фермерских продуктов через интернет. "
    "Основные пользователи: гости (просмотр каталога), клиенты (покупка, личный кабинет) и администраторы (управление). "
    "Анализ конкурентов показал, что многие малые магазины используют социальные сети, что неудобно для учета заказов."
)
add_paragraph(doc, text_21)

# Таблица 1
add_paragraph(doc, "Таблица 1 – Сравнение подходов к созданию интернет-магазина", bold=True)
headers1 = ["Критерий", "Социальные сети", "Готовые CMS", "Разрабатываемое приложение"]
rows1 = [
    ["Управление заказами", "Нет / Ручное", "Сложное", "Простое и наглядное"],
    ["Гибкость дизайна", "Низкая", "Средняя", "Высокая"],
    ["Сложность поддержки", "Низкая", "Высокая", "Средняя"],
    ["Стоимость", "Бесплатно", "Высокая", "Низкая"],
]
create_table(doc, headers1, rows1)
doc.add_paragraph()

add_heading(doc, "2.2 Настройка проектной среды и проектирование базы данных", 2)
text_22 = (
    "Среда разработки: OpenServer Panel (Apache, PHP 8.2, MySQL). "
    "Проект создан командой «composer create-project laravel/laravel farm-shop». "
    "База данных MySQL «farm_shop_db» настроена через файл «.env»."
)
add_paragraph(doc, text_22)

# Таблицы БД
add_paragraph(doc, "Таблица 2 – Структура таблицы «users»", bold=True)
create_table(doc, ["Поле", "Тип", "NULL", "По умолчанию"], [
    ["id", "bigint", "NO", "-"], ["name", "varchar", "NO", "-"], ["email", "varchar", "NO", "-"], 
    ["password", "varchar", "NO", "-"], ["is_admin", "tinyint", "NO", "0"]
])
doc.add_paragraph()

add_paragraph(doc, "Таблица 3 – Структура таблицы «categories»", bold=True)
create_table(doc, ["Поле", "Тип", "NULL", "По умолчанию"], [
    ["id", "bigint", "NO", "-"], ["name", "varchar", "NO", "-"]
])
doc.add_paragraph()

add_paragraph(doc, "Таблица 4 – Структура таблицы «products»", bold=True)
create_table(doc, ["Поле", "Тип", "NULL", "По умолчанию"], [
    ["id", "bigint", "NO", "-"], ["name", "varchar", "NO", "-"], 
    ["price", "decimal(8,2)", "NO", "-"], ["image", "varchar", "YES", "NULL"], 
    ["category_id", "bigint", "NO", "-"]
])
doc.add_paragraph()

add_paragraph(doc, "Таблица 5 – Структура таблицы «orders»", bold=True)
create_table(doc, ["Поле", "Тип", "NULL", "По умолчанию"], [
    ["id", "bigint", "NO", "-"], ["customer_name", "varchar", "NO", "-"], 
    ["phone", "varchar", "NO", "-"], ["address", "text", "NO", "-"],
    ["total", "decimal(8,2)", "NO", "-"], ["status", "varchar", "NO", "new"],
    ["user_id", "bigint", "YES", "NULL"]
])
doc.add_paragraph()

add_heading(doc, "2.3 Реализация системы аутентификации и модуля магазина", 2)
text_23 = (
    "Аутентификация реализована через контроллер «AuthController». "
    "Регистрация включает валидацию данных и хеширование пароля. "
    "Контроллер «MainController» реализует логику работы магазина. "
    "Метод «index()» выводит главную страницу с 4 последними товарами. "
    "Метод «catalog()» выводит все товары с фильтрацией по «category_id». "
    "Корзина реализована через сессию: массив «$cart» хранит id, имя, цену, количество и изображение товара."
)
add_paragraph(doc, text_23)

add_heading(doc, "2.4 Разработка административной панели и тестирование приложения", 2)
text_24 = (
    "Административная панель реализована в «AdminController». "
    "Доступ ограничен Middleware «IsAdmin», проверяющим поле «is_admin» у пользователя. "
    "Методы панели позволяют управлять товарами (CRUD-операции) и заказами (смена статусов, удаление). "
    "Загрузка изображений товаров осуществляется методом «store()» с перемещением файла в директорию «public/images»."
)
add_paragraph(doc, text_24)

doc.add_page_break()

# Заключение
add_heading(doc, "Заключение", 1)
text_conclusion = (
    "В ходе выполнения курсовой работы была достигнута главная цель — спроектировано и разработано веб-приложение «Интернет-магазин фермерских продуктов» на базе PHP-фреймворка Laravel."
    "\n\nВ теоретической части были изучены принципы архитектурного паттерна MVC, возможности PHP-фреймворка Laravel (ORM Eloquent, миграции, Blade, Middleware) и основы проектирования реляционных баз данных."
    "\n\nВ практической части проведен анализ предметной области. Разработана база данных MySQL, состоящая из 4 таблиц. "
    "Реализована система аутентификации и авторизации. Создан модуль каталога с фильтрацией и корзина на основе сессий. "
    "Разработана административная панель для управления товарами и заказами с разделением ролей."
)
add_paragraph(doc, text_conclusion)

doc.add_page_break()

# Список литературы
add_heading(doc, "Список использованных источников", 1)
sources = [
    "Гаевский А. Основы работы в Интернете. Самоучитель. - СПб.: БХВ-Петербург, 2003. - 464 с.",
    "Кузнецова М.В. Разработка Web-приложений на PHP 7. - М.: Диалог-МИФИ, 2018. - 320 с.",
    "Теплов Д.В., Еловой И.А. PHP 7 в подлиннике. - СПб.: БХВ-Петербург, 2018. - 832 с.",
    "Иванов А.П. Современные подходы к разработке веб-приложений на PHP // Журнал «Информационные технологии», 2020.",
    "Официальная документация Laravel [Электронный ресурс]. - Режим доступа: https://laravel.com/docs",
    "Официальная документация MySQL [Электронный ресурс]. - Режим доступа: https://dev.mysql.com/doc/"
]
for i, src in enumerate(sources, 1):
    p = doc.add_paragraph(f"{i}. {src}")
    for run in p.runs:
        run.font.name = 'Times New Roman'
        run.font.size = Pt(14)

doc.add_page_break()

# Приложения
add_heading(doc, "Приложения", 1)

add_paragraph(doc, "Приложение 1", bold=True, align='center')
add_paragraph(doc, "ER – диаграмма базы данных приложения", bold=True)
add_paragraph(doc, "[Здесь должен быть скриншот схемы БД]", align='center')

doc.add_paragraph()

add_paragraph(doc, "Приложение 2", bold=True, align='center')
add_paragraph(doc, "Код контроллера AuthController (фрагмент)", bold=True)
code2 = """
public function register(Request $request) {
    $data = $request->validate([
        'name' => 'required|string|max:255',
        'email' => 'required|email|unique:users',
        'password' => 'required|min:6|confirmed'
    ]);
    $user = User::create([
        'name' => $data['name'],
        'email' => $data['email'],
        'password' => Hash::make($data['password']),
    ]);
    Auth::login($user);
    return redirect('/')->with('success', 'Регистрация успешна!');
}
"""
p = doc.add_paragraph(code2)
p.style = 'No Spacing'
for run in p.runs:
    run.font.name = 'Courier New'
    run.font.size = Pt(10)

doc.add_paragraph()

add_paragraph(doc, "Приложение 3", bold=True, align='center')
add_paragraph(doc, "Код контроллера AdminController (фрагмент)", bold=True)
code3 = """
public function storeProduct(Request $request) {
    $data = $request->validate([
        'name' => 'required',
        'price' => 'required|numeric',
        'category_id' => 'required',
        'image' => 'nullable|image'
    ]);
    if ($request->hasFile('image')) {
        $file = $request->file('image');
        $filename = time() . '_' . $file->getClientOriginalName();
        $file->move(public_path('images'), $filename);
        $data['image'] = 'images/' . $filename;
    }
    Product::create($data);
    return redirect()->route('admin.products');
}
"""
p = doc.add_paragraph(code3)
p.style = 'No Spacing'
for run in p.runs:
    run.font.name = 'Courier New'
    run.font.size = Pt(10)

doc.add_paragraph()

# Ссылки на рисунки
add_paragraph(doc, "Приложение 4", bold=True, align='center')
add_paragraph(doc, "Рисунок 1 - Форма регистрации пользователя", bold=True)
add_paragraph(doc, "[Здесь должен быть скриншот]", align='center')

add_paragraph(doc, "Приложение 5", bold=True, align='center')
add_paragraph(doc, "Рисунок 2 - Форма авторизации пользователя", bold=True)
add_paragraph(doc, "[Здесь должен быть скриншот]", align='center')

add_paragraph(doc, "Приложение 6", bold=True, align='center')
add_paragraph(doc, "Рисунок 3 - Каталог товаров с фильтрацией", bold=True)
add_paragraph(doc, "[Здесь должен быть скриншот]", align='center')

add_paragraph(doc, "Приложение 7", bold=True, align='center')
add_paragraph(doc, "Рисунок 4 - Карточка товара", bold=True)
add_paragraph(doc, "[Здесь должен быть скриншот]", align='center')

add_paragraph(doc, "Приложение 8", bold=True, align='center')
add_paragraph(doc, "Рисунок 5 - Административная панель", bold=True)
add_paragraph(doc, "[Здесь должен быть скриншот]", align='center')

# Сохранение
doc.save('Курсовая_Фермерский_магазин.docx')
print("Документ успешно создан: Курсовая_Фермерский_магазин.docx")